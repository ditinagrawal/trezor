import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Upgrade | Trezor",
};

const UpgradePage = () => {
  return (
    <div>
      <h1>Upgrade Page</h1>
    </div>
  );
};

export default UpgradePage;
