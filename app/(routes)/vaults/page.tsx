import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Vaults | Trezor",
};

const VaultsPage = () => {
  return (
    <main>
      <h1>Vaults</h1>
    </main>
  );
};

export default VaultsPage;
