import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings | Trezor",
};

const SettingsPage = () => {
  return (
    <div>
      <h1>Settings Page</h1>
    </div>
  );
};

export default SettingsPage;
